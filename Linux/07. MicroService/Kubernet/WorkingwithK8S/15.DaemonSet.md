# 11. DeamonSet

## 1. DaemonSet trong Kubernetes là gì?

**DaemonSet** là một loại **controller** trong Kubernetes dùng để đảm bảo **mỗi Node trong cluster sẽ chạy ít nhất một Pod của DaemonSet**. Khi thêm Node mới, DaemonSet sẽ tự động tạo Pod trên Node đó. Ngược lại, khi Node bị xóa, Pod tương ứng cũng sẽ bị xóa.

DaemonSet thường được dùng cho các **agent hoặc daemon** cần chạy trên tất cả Node trong cluster.

Điểm khác biệt chính so với **Deployment** và **StatefulSet**:

**So sánh DaemonSet với các controller khác**

| **Controller** | **Khi nào dùng** |
| --- | --- |
| **Deployment** | Ứng dụng **stateless**, có thể scale lên/xuống dễ dàng |
| **StatefulSet** | Ứng dụng **stateful**, cần Persistent Volume và tên cố định |
| **DaemonSet** | Các **daemon hoặc agent** cần chạy trên mọi Node hoặc một tập Node cụ thể |

---

## 2. Các đặc điểm chính của DaemonSet

- **Một Pod trên mỗi Node**
    
    Mỗi Node sẽ chạy một Pod, đảm bảo tất cả Node đều được “giám sát” hoặc có agent hoạt động.
    
- **Triển khai tự động trên Node mới**
    
    Khi Node mới join cluster, DaemonSet sẽ tự động tạo Pod trên Node đó.
    
- **Có thể giới hạn Node**
    
    Sử dụng `nodeSelector`, `affinity` hoặc `tolerations` để chỉ định Pod chạy trên Node cụ thể.
    
- **Xóa Pod khi Node bị xóa**
    
    Pod của DaemonSet sẽ tự động bị xóa nếu Node bị remove hoặc nếu DaemonSet bị xóa.
    

---

## 3. Các trường hợp sử dụng DaemonSet

DaemonSet thường dùng cho các **ứng dụng daemon hoặc agent** chạy trên mọi Node:

- **Thu thập log**: Fluentd, Logstash, Datadog Agent.
- **Giám sát Node**: Prometheus Node Exporter, cAdvisor.
- **Quản lý mạng**: Calico, Cilium, Weave Net.
- **Quản lý bảo mật hoặc cấu hình Node**: Falco, Sysdig, các tool quản lý cấu hình.

---

## 4. DaemonSet101
Ví dụ: Triển khai một agent thu thập log (fluentd) trên tất cả các Node. Tích hợp tolerations để cho phép Pod được schedule lên cả Control Plane Node (vốn có mặc định taint NoSchedule)
```YAML
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: fluentd-log-collector
  namespace: kube-system
  labels:
    app: fluentd
spec:
  selector:
    matchLabels:
      app: fluentd
  template:
    metadata:
      labels:
        app: fluentd
    spec:
      tolerations:
      # Cấu hình bỏ qua Taint của Control Plane để force schedule
      - key: node-role.kubernetes.io/control-plane
        operator: Exists
        effect: NoSchedule
      containers:
      - name: fluentd
        image: fluent/fluentd:v1.14-1
        resources:
          limits:
            memory: 200Mi
          requests:
            cpu: 100m
            memory: 200Mi
```
- Apply:
```
root@k8s-master:/home/ducmanh/k8s# kubectl apply -f test.yaml
daemonset.apps/fluentd-log-collector created
```

- Xem DaemonSet:

```bash
root@k8s-master:/home/ducmanh/k8s# kubectl get ds fluentd-log-collector -n kube-system
NAME                    DESIRED   CURRENT   READY   UP-TO-DATE   AVAILABLE   NODE SELECTOR   AGE
fluentd-log-collector   3         3         3       3            3           <none>          25s
```

- Xem vị trí schedule
```
root@k8s-master:/home/ducmanh/k8s# kubectl get pods -n kube-system -l app=fluentd -o wide
NAME                          READY   STATUS    RESTARTS   AGE   IP            NODE           NOMINATED NODE   READINESS GATES
fluentd-log-collector-46rpk   1/1     Running   0          50s   10.244.0.5    k8s-master     <none>           <none>
fluentd-log-collector-sg4vm   1/1     Running   0          50s   10.244.2.29   k8s-worker-1   <none>           <none>
fluentd-log-collector-v9ql8   1/1     Running   0          50s   10.244.1.24   k8s-worker-2   <none>           <none>
```
- Phản ứng hệ thống khi mất Node
```
root@k8s-master:/home/ducmanh/k8s# kubectl cordon k8s-worker-2
node/k8s-worker-2 cordoned
root@k8s-master:/home/ducmanh/k8s# kubectl delete pod fluentd-log-collector-v9ql8 -n kube-system
pod "fluentd-log-collector-v9ql8" deleted
```
```
root@k8s-master:/home/ducmanh/k8s# kubectl get pods -n kube-system -l app=fluentd -o wide
NAME                          READY   STATUS    RESTARTS   AGE     IP            NODE           NOMINATED NODE   READINESS GATES
fluentd-log-collector-46rpk   1/1     Running   0          2m49s   10.244.0.5    k8s-master     <none>           <none>
fluentd-log-collector-gcgxj   1/1     Running   0          11s     10.244.1.25   k8s-worker-2   <none>           <none>
fluentd-log-collector-sg4vm   1/1     Running   0          2m49s   10.244.2.29   k8s-worker-1   <none>           <none>
```
Phân tích Root Cause: - DaemonSet Controller phát hiện k8s-worker-2 đang thiếu Pod nên lập tức ra lệnh tạo Pod mới (nxt88).
- Pod nxt88 kẹt ở trạng thái Pending do Node k8s-worker-2 đã bị cordon (SchedulingDisabled).
- TUYỆT ĐỐI KHÔNG có hiện tượng Pod nxt88 bị đẩy sang k8s-worker-1 hay master như cơ chế của Deployment/StatefulSet. Điều này chứng minh tính toàn vẹn của nguyên tắc kiến trúc 1-Pod-Per-Node.

Xem chi tiết DaemonSet:

```bash
root@k8s-master:/home/ducmanh/k8s# kubectl describe daemonset kube-proxy -n kube-system
Name:           kube-proxy
Selector:       k8s-app=kube-proxy
Node-Selector:  kubernetes.io/os=linux
Labels:         k8s-app=kube-proxy
Annotations:    deprecated.daemonset.template.generation: 1
Desired Number of Nodes Scheduled: 3
Current Number of Nodes Scheduled: 3
Number of Nodes Scheduled with Up-to-date Pods: 3
Number of Nodes Scheduled with Available Pods: 3
Number of Nodes Misscheduled: 0
Pods Status:  3 Running / 0 Waiting / 0 Succeeded / 0 Failed
Pod Template:
  Labels:           k8s-app=kube-proxy
  Service Account:  kube-proxy
  Containers:
   kube-proxy:
    Image:      registry.k8s.io/kube-proxy:v1.29.15
    Port:       <none>
    Host Port:  <none>
    Command:
      /usr/local/bin/kube-proxy
      --config=/var/lib/kube-proxy/config.conf
      --hostname-override=$(NODE_NAME)
...
```

